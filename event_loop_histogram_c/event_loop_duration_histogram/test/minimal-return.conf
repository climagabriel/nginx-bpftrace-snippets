master_process on;
daemon off;
worker_processes 42;
error_log stderr debug;
events {}

http {

    access_log /dev/stderr;
    server {
            server_name _;
            listen 127.0.0.1:8080;
            location / {
                return 200;
            }
    }
}

